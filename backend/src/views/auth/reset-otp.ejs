<%- include('../partials/header', { title: 'Nhập mã xác nhận' }) %>

<div class="container py-4 d-flex justify-content-center">
  <div class="col-md-6 col-lg-5">
    <div class="card border-0 shadow-sm mb-3">
      <div class="card-body">
        <h1 class="h4 mb-3 text-primary fw-bold">🔐 Nhập mã xác nhận</h1>

        <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert alert-danger"><%= error %></div>
        <% } %>
        <% if (typeof message !== 'undefined' && message) { %>
          <div class="alert alert-success"><%= message %></div>
        <% } %>

        <!-- Form xác nhận OTP -->
        <form method="post" action="/new-password" class="mb-3">
          <div class="mb-3">
            <label class="form-label">Email</label>
            <!-- Hiển thị cho người dùng -->
            <input
              type="email"
              class="form-control"
              value="<%= email %>"
              placeholder="Email đã nhận mã"
              readonly
            >
            <!-- Ẩn, dùng để gửi lên server -->
            <input type="hidden" name="email" value="<%= email %>">
          </div>

          <div class="mb-3">
            <label class="form-label">Mã xác nhận (OTP)</label>
            <input
              type="text"
              name="code"
              class="form-control"
              placeholder="Nhập mã 6 số"
              required
            >
          </div>

          <button class="btn btn-primary w-100">Xác nhận</button>
        </form>

        <!-- Form gửi lại mã -->
        <form method="post" action="/new-password/resend">
          <input type="hidden" name="email" value="<%= email %>">
          <button class="btn btn-outline-secondary w-100">
            Gửi lại mã nếu chưa nhận được
          </button>
        </form>

        <p class="mt-3 small mb-0">
          Quay lại <a href="/forgot-password">trang quên mật khẩu</a>
        </p>
      </div>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>
